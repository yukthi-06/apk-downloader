# User Prompt
Copy About page layout from 'X:\Projects_X\0_Active\8_Android_APK\01_GPS\Travel_Route_Helper_APK_GITHUB'

# Copy About Page Layout from Travel Route Helper

Copy the layout design of the About page from the Travel Route Helper project to the current APK Downloader project, updating resources (like the app icon, colors, and layout configurations) to match.

## User Review Required

> [!NOTE]
> The layout will use the current app's existing icon (`@drawable/app_icon`) instead of Travel Route Helper's launcher image, and we will define the required color resources (`primary`, `text_primary`, `text_secondary`, `divider`) in `colors.xml`.

## Open Questions

None.

## Proposed Changes

### Resources

#### [MODIFY] [colors.xml](file:///x:/Projects_X/0_Active/8_Android_APK/02_Stable/APK_Downloader_APK_GITHUB/app/src/main/res/values/colors.xml)
- Define new colors: `primary`, `text_primary`, `text_secondary`, `divider`.

#### [MODIFY] [activity_about.xml](file:///x:/Projects_X/0_Active/8_Android_APK/02_Stable/APK_Downloader_APK_GITHUB/app/src/main/res/layout/activity_about.xml)
- Replace contents with the CoordinatorLayout structure from `Travel_Route_Helper_APK_GITHUB`.
- Adapt the toolbar theme to `@style/Theme.ApkDownloader`.
- Use `@drawable/app_icon` for the main `ImageView`.
- Update hardcoded version text from "Version 1.0.0" to "Version 1.1" to match the current app description.

## Verification Plan

### Manual Verification
- Build/inspect layout XML to verify UI structure and compilation success.
